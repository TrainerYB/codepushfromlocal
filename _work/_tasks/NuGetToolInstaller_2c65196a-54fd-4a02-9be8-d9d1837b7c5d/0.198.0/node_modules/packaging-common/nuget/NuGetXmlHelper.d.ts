import { INuGetXmlHelper } from "./INuGetXmlHelper";
export declare class NuGetXmlHelper implements INuGetXmlHelper {
    private _nugetConfigPath;
    constructor(_nugetConfigPath: string);
    SetApiKeyInNuGetConfig(source: string, apiKey: string): void;
    AddSourceToNuGetConfig(name: string, source: string, username?: string, password?: string): void;
    RemoveSourceFromNuGetConfig(name: string): void;
    private _addCredentialsToSource(xml, name, username, password);
    private _removeSourceCredentials(xml, name);
    /**
     * Validates the xml element is a configuration element
     * @throws Will throw an error if the xml is not a configuration element
     * @param xml Xml Element
     */
    private static _validateXmlIsConfiguration(xml);
    /**
     * Gets the last element in xml that matches elementName. If no existing element is found,
     * one will be created on the root of xml
     * @param xml Xml Element to search
     * @param elementName Element name to return or create
     */
    private static _getOrCreateLastElement(xml, elementName);
    private static _updateXmlFile(xmlPath, updateFn);
    private static _nuGetEncodeElementName(name);
    private static _nuGetEncodeCharater(char);
}
